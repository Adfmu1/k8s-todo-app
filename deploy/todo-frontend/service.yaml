apiVersion: v1
kind: Service
metadata:
  name: todo-frontend-svc
spec:
  type: ClusterIP
  selector:
    app: todo-frontend
  ports:
    - name: http
      protocol: TCP
      port: 8989
      targetPort: 8989