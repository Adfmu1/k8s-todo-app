apiVersion: batch/v1
kind: CronJob
metadata:
  name: todo-cronjob
spec:
  schedule: "0 */1 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: todo-cronjob
            image: adfmu/cronjob:latest
          restartPolicy: OnFailure
